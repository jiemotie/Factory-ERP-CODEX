# Web Management Console

Web 管理端用于后台管理、主数据维护、订单录入、工序跟踪和报表查看。

## 数据访问原则

- 只能通过 `backend/` 暴露的统一 API 读写业务数据。
- 不直接连接数据库。
- 不在浏览器本地持久化权威业务状态；缓存仅用于性能优化，必须可被后端数据覆盖。

## 建议技术栈

React 或 Vue 均可。当前目录保留 `src/` 作为后续前端工程入口。
