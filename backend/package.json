{
  "name": "factory-erp-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Factory ERP unified backend API prototype",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
